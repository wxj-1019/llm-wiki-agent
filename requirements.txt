# litellm 1.82.7–1.82.8 were compromised in a supply chain attack (Mar 2026).
# Pin to a verified safe version. See: https://github.com/SamurAIGPT/llm-wiki-agent/issues/41
litellm~=1.83.10
networkx~=3.6.1

# PostgreSQL + pgvector (optional — used when config/database.yaml sets backend: postgresql)
psycopg2-binary>=2.9
pgvector>=0.3

# Agent Kit: auto-generate MCP Server & Kimi Skill from wiki
mcp>=1.2.0
jinja2>=3.1.0

# File watcher for auto-ingest
watchdog>=3.0.0

# API server (not in pyproject.toml because it's optional headless usage)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pyyaml>=6.0.0

# Document conversion (PDF/docx/pptx → markdown)
markitdown[all]>=0.1.5,<0.2.0

# Web article extraction (primary engine for web_fetcher.py)
trafilatura>=1.12.0

# Adaptive extraction fallback (site-change resilient, optional)
scrapling>=0.1.0

# RSS/Atom feed parsing (used by rss_fetcher.py)
feedparser>=6.0.11

# Async HTTP client (used by rss_fetcher.py, web_fetcher.py)
httpx[http2]>=0.28.0

# PDF to Markdown conversion (used by arxiv_fetcher.py)
pymupdf4llm>=0.1.0

# JS rendering for web_fetcher.py SPA/JS-heavy pages
playwright>=1.40.0
